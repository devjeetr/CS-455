Submission: John Chen, Devjeet Roy

use run{CTI, Test1, Test2, Test4}.sh to run this supplied tests

By default, poison reverse is turned off. To switch it on, you will need to edit
the run{*}.sh files